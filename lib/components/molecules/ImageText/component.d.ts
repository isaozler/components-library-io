import IProps from './component.types';
declare const Component: ({ className, title, sizes, contents, hasSpaceBetween, isVertical, image: { caption, image, fit, href, newWindow }, linkWrapper, innerRef, }: IProps) => JSX.Element;
export default Component;
