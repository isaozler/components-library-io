import { ComponentStory, ComponentMeta } from '@storybook/react';
declare const _default: ComponentMeta<({ className, image, alt, title, width, height, href, newWindow, fit, linkWrapper, innerRef, }: import("./component.types").IProps) => JSX.Element>;
export default _default;
export declare const Default: ComponentStory<({ className, image, alt, title, width, height, href, newWindow, fit, linkWrapper, innerRef, }: import("./component.types").IProps) => JSX.Element>;
