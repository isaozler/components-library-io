import IProps from './component.types';
declare const Component: ({ className, image, alt, title, width, height, href, newWindow, fit, linkWrapper, innerRef, }: IProps) => JSX.Element;
export default Component;
